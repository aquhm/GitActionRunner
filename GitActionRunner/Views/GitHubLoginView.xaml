<Page x:Class="GitActionRunner.Views.GitHubLoginView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:local="clr-namespace:GitActionRunner.Views">
    <!-- 기존 내용은 그대로 유지 -->

    <Grid Margin="20">
        <StackPanel>
            <TextBlock Text="GitHub 계정 연동" 
                       FontSize="24" 
                       Margin="0,0,0,20"/>
                       
            <Border BorderBrush="LightGray" 
                    BorderThickness="1" 
                    Padding="20" 
                    Margin="0,0,0,20"
                    CornerRadius="4">
                <StackPanel>
                    <TextBlock Text="연동 상태" 
                               Margin="0,0,0,10"/>
                    <TextBlock Text="{Binding ConnectionStatus}" 
                               FontWeight="Bold"/>
                    
                    <TextBox Text="{Binding AccessToken, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource DefaultTextBox}"
                             Width="300"
                             Margin="0,0,0,10"/>
                    <Button Content="토큰으로 로그인"
                            Command="{Binding LoginWithTokenCommand}"
                            Style="{StaticResource ActionButton}"/>
                    
                    <!-- <Button Content="GitHub 로그인"  -->
                    <!--         Command="{Binding LoginCommand}" -->
                    <!--         Width="200" -->
                    <!--         Height="40" -->
                    <!--         Margin="0,20,0,0" -->
                    <!--         Visibility="{Binding IsNotConnected, Converter={StaticResource BoolToVisibilityConverter}}"/> -->
                            
                    <Button Content="연동 해제" 
                            Command="{Binding LogoutCommand}"
                            Width="200"
                            Height="40"
                            Margin="0,20,0,0"
                            Visibility="{Binding IsConnected, Converter={StaticResource BoolToVisibilityConverter}}"/>
                </StackPanel>
            </Border>
            
            <Border BorderBrush="LightGray" 
                    BorderThickness="1" 
                    Padding="20"
                    CornerRadius="4"
                    Visibility="{Binding IsConnected, Converter={StaticResource BoolToVisibilityConverter}}">
                <StackPanel>
                    <TextBlock Text="사용자 정보" 
                               Margin="0,0,0,10"/>
                    <StackPanel Orientation="Horizontal" 
                               Margin="0,0,0,5">
                        <TextBlock Text="사용자명: " 
                                   Width="100"/>
                        <TextBlock Text="{Binding UserName}"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="이메일: " 
                                   Width="100"/>
                        <TextBlock Text="{Binding Email}"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</Page>